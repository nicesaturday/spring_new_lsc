package com.ajaxpro.api.app.controller;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;

@Controller
public class DetailController {
	
	/*
	 * @GetMapping("food/detail") public String detailPage(String title, String lat,
	 * String lng, String description, String img, Model model) {
	 * 
	 * model.addAttribute("title",title); model.addAttribute("lat" , lat);
	 * model.addAttribute("lng" , lng); model.addAttribute("description" ,
	 * description); model.addAttribute("img" , img);
	 * 
	 * 
	 * String de = description.replaceAll("\n", "");
	 * 
	 * 
	 * 
	 * 
	 * return "busan/detail"; }
	 */
}
